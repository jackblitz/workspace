# Root BUILD file for the hot reload sample
package(default_visibility = ["//visibility:public"])

# Build configurations
config_setting(
    name = "debug_mode",
    values = {"compilation_mode": "dbg"},
)

config_setting(
    name = "release_mode",
    values = {"compilation_mode": "opt"},
)